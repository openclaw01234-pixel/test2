{
  "name": "math-stars",
  "version": "1.0.0",
  "private": true,
  "description": "Math Stars - Fun math practice game for kids",
  "scripts": {
    "prepare:www": "node scripts/prepare-math-www.js",
    "icons": "node scripts/make-math-icons.js",
    "init:android": "npm run prepare:www && npx cap add android",
    "sync:android": "npm run prepare:www && npx cap sync android",
    "build:android": "npm run sync:android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/cli": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "pngjs": "^7.0.0"
  }
}
